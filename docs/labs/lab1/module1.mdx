---
sidebar_position: 1
---

# Module:1 Programming Concept: Input → Process → Output

การเขียนโปรแกรมคือการ “บอกคอมพิวเตอร์ให้ทำตามขั้นตอน”  
โดยทุกโปรแกรมไม่ว่าจะซับซ้อนแค่ไหน ก็มักประกอบด้วยแนวคิดพื้นฐาน 3 ส่วนนี้เสมอ

---

## 1. Input (ข้อมูลนำเข้า)

**Input** คือข้อมูลที่โปรแกรมต้องใช้ในการทำงาน  เช่น ตัวเลขที่ผู้ใช้พิมพ์เข้ามา, ข้อมูลจากไฟล์, หรือข้อความจากผู้ใช้งาน

**ตัวอย่าง:**
- ผู้ใช้กรอกเลข 10 และ 20 เพื่อให้โปรแกรมบวกกัน  
- โปรแกรมอ่านข้อมูล “ราคาสินค้า” จากไฟล์  
- เว็บไซต์รับ “รหัสผ่าน” ที่ผู้ใช้พิมพ์

```js
// ตัวอย่าง Input
const a = 10;
const b = 20;
```

---

## 2. Process (กระบวนการทำงาน)

**Process** คือขั้นตอนที่คอมพิวเตอร์นำข้อมูลจาก Input  มาคำนวณ วิเคราะห์ หรือประมวลผลตามตรรกะที่เรากำหนด

ในมุมของโปรแกรมเมอร์ เราเรียกส่วนนี้ว่า “Logic” คือการสั่งให้เครื่องทำงานตามเงื่อนไขหรือสูตรที่เราคิดไว้

### ตัวอย่างแนวคิด

- บวกเลข `10 + 20`
- ตรวจสอบว่า “รหัสผ่าน” ตรงกับที่บันทึกไว้หรือไม่
- คำนวณส่วนลดจากราคาสินค้า
- หาค่าเฉลี่ยคะแนนของนักเรียน

### ตัวอย่างโค้ด

```js
// Input
const price = 100;
const discount = 0.2;

// Process
const total = price - price * discount;

// Output
console.log("ราคาหลังหักส่วนลด =", total);
```


---

## 3. Output (ผลลัพธ์)

**Output** คือผลลัพธ์ที่ได้จากการประมวลผล (Process)  อาจแสดงออกมาในหลายรูปแบบ เช่น

- แสดงข้อความบนหน้าจอ
- ส่งข้อมูลกลับไปยังผู้ใช้
- บันทึกผลลงในไฟล์หรือฐานข้อมูล

### ตัวอย่าง

```js
const name = "Panuwat";
const year = 2025;

// Output
console.log("ยินดีต้อนรับ", name);
console.log("ปีปัจจุบันคือ", year);
```

```yaml
ยินดีต้อนรับ Panuwat
ปีปัจจุบันคือ 2025
```

### หรือถ้าเราอยากให้ Output ไปเก็บในไฟล์ ก็ทำได้เช่นกัน (ใน Node.js)
```js
import fs from "fs";

fs.writeFileSync("message.txt", "Hello from Node.js!");
console.log("บันทึกไฟล์เรียบร้อยแล้ว");
```

<iframe
  src="https://stackblitz.com/edit/nodejs-starter?embed=1&file=index.js"
  width="100%"
  height="450"
></iframe>


---

## 🧭 Summary

ใน Module นี้เราได้เรียนรู้พื้นฐานของการเขียนโปรแกรม  
ผ่านแนวคิดหลัก 3 ส่วน คือ **Input → Process → Output**

| ส่วน | หน้าที่ | ตัวอย่าง |
|------|----------|-----------|
| **Input** | รับข้อมูลจากผู้ใช้ | กรอกเลข 10 และ 20 |
| **Process** | ประมวลผลข้อมูล | นำ 10 + 20 |
| **Output** | แสดงผลลัพธ์กลับ | แสดงคำตอบ 30 |

> ทุกโปรแกรมไม่ว่าจะซับซ้อนแค่ไหน ก็เกิดจากลำดับความคิดนี้เสมอ  
> ถ้าเข้าใจโครงสร้างนี้ เราจะสามารถเขียนโปรแกรมภาษาใดก็ได้ในอนาคต

---

## 💡 Mini Challenge

ลองเขียนโปรแกรมง่าย ๆ ในไฟล์ `age.js`  
เพื่อคำนวณอายุของคุณจากปีเกิด

**ตัวอย่าง Output:**
```yaml
> อายุของคุณคือ : 26 ปี
```

```yaml
> Hint: ใช้ `new Date().getFullYear()` เพื่อหาปีปัจจุบัน  
> แล้วนำไปลบกับค่าปีเกิดที่ผู้ใช้ป้อนเข้ามา
```